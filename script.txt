1- from sklearn.datasets import load_iris
2- from sklearn.model_selection import train _test_split
3- from sklearn.ensemble import RandomForestClassifier
4- X, y = load_iris(return_X_y=True); X_train, X_test, y_train, y_test=train_test_split(X, y, test_size=0.2)
5- model = RandomForesetClassifier().fit(X_train, y_train); print(Accuracy:,model.score(X_test,y_test))

